"""Daily BOE scraper landing raw PDFs in lh_esp_origination (Fabric OneLake).

Bronze-only ingestion. Silver/gold tiers, extraction, and consumer-facing
APIs are out of scope for this package — see the project ADRs for the
broader architecture.
"""

__version__ = "0.1.0"
